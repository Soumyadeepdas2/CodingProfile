class Solution {
  public:
    int missingNum(vector<int>& arr) {
        // code here
        long n = arr.size()+1;
        long ans = n*(n+1)/2;
        long sum = 0;
        for(int i = 0; i < arr.size(); i++){
            sum +=arr[i];
        }
        return ans - sum;
    }
};